services:
  solardecision-hub:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      PDF_RENDERER: ${PDF_RENDERER:-local}
    restart: unless-stopped
